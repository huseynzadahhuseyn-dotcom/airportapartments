/**
 * Premium Residence (listing `bina` — primary set). Direct HTTPS URLs (e.g. i.postimg.cc).
 * Pairs with `bina-residence-images.js` for extra shots; both concatenate on the card + gallery.
 */
(function () {
  "use strict";
  window.PREMIUM_RESIDENCE_IMAGE_URLS = [];
})();
