/**
 * Premium Residence — primary photo set for listing `bina` (same pattern as `js/cozy-studio-images.js`).
 * Direct HTTPS URLs only (e.g. i.postimg.cc/…). Pairs with `bina-residence-images.js` (appended after this list).
 * Feeds: apartment cards, apartment-detail, homepage gallery (merged with bina URLs there too).
 */
(function () {
  "use strict";

  window.PREMIUM_RESIDENCE_IMAGE_URLS = [
  ];
})();
