/**
 * Airport Haven / Comfort Residence — listing `haven` (same pattern as `js/cozy-studio-images.js`).
 * Direct HTTPS URLs. Feeds: cards, apartment-detail, homepage gallery group `gallery_group_haven`.
 */
(function () {
  "use strict";

  window.AIRPORT_HAVEN_IMAGE_URLS = [
  ];
})();
