/**
 * Bina / Premium corridor — extra photos for listing `bina` (same pattern as `js/cozy-studio-images.js`).
 * Appended after `PREMIUM_RESIDENCE_IMAGE_URLS` on cards, detail, and in the premium gallery block.
 */
(function () {
  "use strict";

  window.BINA_RESIDENCE_IMAGE_URLS = [
  ];
})();
